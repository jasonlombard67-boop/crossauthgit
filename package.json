{
  "name": "teleauth",
  "version": "2.0.0",
  "description": "Zero-friction cross-device authentication via Telegram deep-link OTP — Vercel edition",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate deploy",
    "db:seed": "node prisma/seed.js",
    "db:studio": "prisma studio",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0",
    "bcrypt": "^6.0.0",
    "cookie": "^1.1.1",
    "jsonwebtoken": "^9.0.2",
    "node-fetch": "^3.3.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "prisma": "^5.10.0",
    "vercel": "^54.17.3"
  },
  "engines": {
    "node": "24.x"
  }
}
